import PageStub from "../components/PageStub";

export const metadata = { title: "Contact | Koda Allison" };

export default function ContactPage() {
  return (
    <PageStub
      route="/contact"
      file="mail_client.sh"
      summary="terminal prompt with blinking cursor; preserves the existing resend integration."
    />
  );
}
