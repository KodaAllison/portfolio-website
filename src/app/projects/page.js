import PageStub from "../components/PageStub";

export const metadata = { title: "Projects | Koda Allison" };

export default function ProjectsPage() {
  return (
    <PageStub
      route="/projects"
      file="projects.tsx"
      summary="terminal-window cards with --tag flags and file-path titles, migrating the existing project set."
    />
  );
}
